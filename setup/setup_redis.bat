@echo off
:: Redis setup must be performed on Linux.
:: This script only exists to inform Windows users.

echo WARNING: Redis setup must be run on Linux.
echo Please run setup_redis.sh on the target Linux system.
